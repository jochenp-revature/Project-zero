package com.revature.models;

public enum Role {
	/**
	 * it's important that the casing (upper/lower) of these variables is equal to
	 * how the ENUMS are cased in the custom ENUM type we defined in the database
	 */
	Admin, Employee, Customer
}
